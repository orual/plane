# Test requirements -- agent UX (phases 1-8)

This document maps every acceptance criterion from the [agent UX design](../../design-plans/2026-02-16-agent-ux.md) to its test coverage. Each criterion appears in exactly one of two sections: automated test coverage or human verification required.

---

## Automated test coverage

### agent-ux.AC1: Agent management UI

| Criterion                             | Type        | Test File                                                      | Phase | Description                                                                                                                                                                                          |
| ------------------------------------- | ----------- | -------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| agent-ux.AC1.1 (service)              | unit        | `apps/web/hw/services/agent.service.test.ts`                   | 6     | `listAgentProfiles` calls GET `/api/workspaces/{slug}/agents/` and returns parsed response. Error handling throws `error.response.data`.                                                             |
| agent-ux.AC1.1 (store fetch)          | unit        | `apps/web/hw/store/agent/agent-profile.store.test.ts`          | 6     | `fetchProfiles` populates `profiles` map keyed by ID with correct fields including `agent_type`, `display_name`, and `is_active`.                                                                    |
| agent-ux.AC1.1 (store lifecycle)      | integration | `apps/web/hw/store/agent/agent-profile.store.test.ts`          | 6     | Full CRUD lifecycle: fetch, create, update, remove -- verify map state at each step.                                                                                                                 |
| agent-ux.AC1.2 (service create)       | unit        | `apps/web/hw/services/agent.service.test.ts`                   | 6     | `createAgentProfile` calls POST `/api/workspaces/{slug}/agents/` with payload and returns response including `api_token`.                                                                            |
| agent-ux.AC1.2 (store create)         | unit        | `apps/web/hw/store/agent/agent-profile.store.test.ts`          | 6     | `createProfile` adds new profile to map AND stores `apiToken` for one-time display.                                                                                                                  |
| agent-ux.AC1.3 (service detail)       | unit        | `apps/web/hw/services/agent.service.test.ts`                   | 6     | `getAgentProfile` calls GET `/api/workspaces/{slug}/agents/{id}/` and returns parsed profile. `updateAgentProfile` calls PATCH with partial payload.                                                 |
| agent-ux.AC1.4 (seed exists)          | unit        | `apps/api/plane/tests/unit/hw/test_builtin_agent_seed.py`      | 1     | After `workspace_seed` runs, a built-in `AgentProfile` exists with `agent_type="builtin"`, `is_active=True`, and a bot user with `bot_type=BotTypeEnum.AGENT`.                                       |
| agent-ux.AC1.4 (idempotency)          | unit        | `apps/api/plane/tests/unit/hw/test_builtin_agent_seed.py`      | 1     | Running the seed task twice for the same workspace does not create duplicate built-in agent profiles.                                                                                                |
| agent-ux.AC1.4 (workspace member)     | unit        | `apps/api/plane/tests/unit/hw/test_builtin_agent_seed.py`      | 1     | The built-in agent's bot user is added as a workspace member.                                                                                                                                        |
| agent-ux.AC1.4 (model)                | unit        | `apps/api/plane/tests/unit/hw/test_agent_models.py`            | 1     | A built-in agent profile can be created with `agent_type="builtin"`.                                                                                                                                 |
| agent-ux.AC1.5 (service deactivate)   | unit        | `apps/web/hw/services/agent.service.test.ts`                   | 6     | `updateAgentProfile` calls PATCH with `{ is_active: false }` payload.                                                                                                                                |
| agent-ux.AC1.5 (store deactivate)     | unit        | `apps/web/hw/store/agent/agent-profile.store.test.ts`          | 6     | `updateProfile` with `{ is_active: false }` updates the profile in the map to reflect deactivated state.                                                                                             |
| agent-ux.AC1.5 (deactivation routing) | contract    | `apps/api/plane/tests/contract/hw/test_agent_conversations.py` | 4     | Mention routing skips agents with `is_active=False` (existing behaviour preserved by `_detect_agent_mentions` filtering on active profiles).                                                         |
| agent-ux.AC1.6 (route access)         | integration | `apps/web/hw/store/agent/agent-profile.store.test.ts`          | 6     | Non-admin access is gated at the route page level via `allowPermissions([EUserPermissions.ADMIN])`. Test verified via store error propagation when the backend returns 403.                          |
| agent-ux.AC1.7 (duplicate name)       | contract    | `apps/api/plane/tests/contract/hw/test_agent_registration.py`  | --    | Existing contract tests already verify that creating an agent with a duplicate display name within the same workspace returns a validation error. No new test needed; this is pre-existing coverage. |
| agent-ux.AC1.7 (store error)          | unit        | `apps/web/hw/store/agent/agent-profile.store.test.ts`          | 6     | When the service throws a validation error for duplicate name, the store propagates the error and does not modify the profiles map.                                                                  |

### agent-ux.AC2: Agent mention autocomplete

| Criterion                          | Type        | Test File                                                      | Phase | Description                                                                                                                                                                                |
| ---------------------------------- | ----------- | -------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| agent-ux.AC2.1 (mention types)     | unit        | `apps/web/hw/hooks/use-additional-editor-mention.test.tsx`     | 7     | When `enableAdvancedMentions` is true, `editorMentionTypes` includes `"agent_mention"` and `updateAdditionalSections` returns an "Agents" section with correct `TMentionSuggestion` shape. |
| agent-ux.AC2.1 (integration)       | integration | `apps/web/hw/hooks/use-editor-mention-integration.test.tsx`    | 7     | Mock `searchEntity` returning both `user_mention` and `agent_mention` results. Verify `fetchMentions` returns both "Users" and "Agents" sections.                                          |
| agent-ux.AC2.2 (suggestions)       | unit        | `apps/web/hw/hooks/use-additional-editor-mention.test.tsx`     | 7     | Each suggestion shows matching agent display name with a bot icon and type badge (subTitle).                                                                                               |
| agent-ux.AC2.2 (search service)    | unit        | `apps/web/hw/services/agent.service.test.ts`                   | 7     | `searchAgents` calls the correct search endpoint with query parameters and returns the parsed response.                                                                                    |
| agent-ux.AC2.3 (insertion)         | unit        | `apps/web/hw/hooks/use-additional-editor-mention.test.tsx`     | 7     | Each suggestion has `entity_name: "agent_mention"` and `entity_identifier` set to the agent profile ID.                                                                                    |
| agent-ux.AC2.3 (integration)       | integration | `apps/web/hw/hooks/use-editor-mention-integration.test.tsx`    | 7     | Agent suggestion items have `entity_name: "agent_mention"` and correct `entity_identifier` for inserting `@agent-name`.                                                                    |
| agent-ux.AC2.4 (builtin dispatch)  | contract    | `apps/api/plane/tests/contract/hw/test_agent_conversations.py` | 4     | Posting a comment with `@builtin-agent-name` dispatches `builtin_agent_execute_task` (not `agent_webhook_send_task`). Mock both tasks and verify `.delay()` is called for the correct one. |
| agent-ux.AC2.4 (backend routing)   | unit        | `apps/api/plane/tests/unit/test_agent_mentions.py`             | 7     | Comment with `@builtin-agent` dispatches the built-in execution task (not webhook). Celery task dispatch is mocked.                                                                        |
| agent-ux.AC2.5 (external dispatch) | contract    | `apps/api/plane/tests/contract/hw/test_agent_conversations.py` | 4     | Posting a comment with `@external-agent-name` dispatches `agent_webhook_send_task` (existing behaviour preserved).                                                                         |
| agent-ux.AC2.5 (backend routing)   | unit        | `apps/api/plane/tests/unit/test_agent_mentions.py`             | 7     | Comment with `@external-agent` dispatches webhook task (existing behaviour preserved). Celery task dispatch is mocked.                                                                     |
| agent-ux.AC2.6 (bot comment)       | unit        | `apps/api/plane/tests/unit/hw/test_builtin_agent_task.py`      | 4     | When the run is issue-scoped, a `response` activity auto-creates an `IssueComment` with `external_source="agent"` and the agent's bot user as actor.                                       |
| agent-ux.AC2.7 (deactivated)       | contract    | `apps/api/plane/tests/contract/hw/test_agent_conversations.py` | 4     | Mentioning a deactivated agent does not trigger execution or webhook. Verified by existing `_detect_agent_mentions` filtering plus new test asserting no task dispatch for inactive agent. |
| agent-ux.AC2.7 (backend inactive)  | unit        | `apps/api/plane/tests/unit/test_agent_mentions.py`             | 7     | Comment with `@inactive-agent` (deactivated) does not trigger any task dispatch.                                                                                                           |
| agent-ux.AC2.7 (hook empty)        | unit        | `apps/web/hw/hooks/use-additional-editor-mention.test.tsx`     | 7     | When the mock response contains no agents (empty array), `updateAdditionalSections` returns `{ sections: [] }`.                                                                            |
| agent-ux.AC2.8 (CE stub)           | unit        | `apps/web/hw/hooks/use-additional-editor-mention.test.tsx`     | 7     | When `enableAdvancedMentions` is false, `editorMentionTypes` is `["user_mention"]` and sections are empty (CE behaviour).                                                                  |

### agent-ux.AC3: Agent chat UI

| Criterion                               | Type      | Test File                                                      | Phase | Description                                                                                                                                                                    |
| --------------------------------------- | --------- | -------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| agent-ux.AC3.1 (panel visibility)       | component | `apps/web/hw/components/agent/chat/agent-chat-panel.test.tsx`  | 8     | Panel renders when `isPanelOpen` is true, hidden when false. Trigger button toggles panel. Panel persists across simulated navigation.                                         |
| agent-ux.AC3.1 (store persistence)      | unit      | `apps/web/hw/store/agent/agent-conversation.store.test.ts`     | 8     | `isPanelOpen` persists through simulated actions (open, navigate mock, verify still open).                                                                                     |
| agent-ux.AC3.2 (service send)           | unit      | `apps/web/hw/services/agent-conversation.service.test.ts`      | 8     | `sendMessage` calls correct endpoint with payload and returns parsed response. `createConversation` calls correct endpoint.                                                    |
| agent-ux.AC3.2 (store send)             | unit      | `apps/web/hw/store/agent/agent-conversation.store.test.ts`     | 8     | `sendMessage` calls service, optimistically adds message to store, and updates on response.                                                                                    |
| agent-ux.AC3.2 (panel send)             | component | `apps/web/hw/components/agent/chat/agent-chat-panel.test.tsx`  | 8     | Sending a message calls `agentConversationStore.sendMessage` with correct args. If no active conversation, `createConversation` is called first.                               |
| agent-ux.AC3.2 (message triggers agent) | contract  | `apps/api/plane/tests/contract/hw/test_agent_conversations.py` | 4     | `POST` to conversation messages endpoint creates a user message, an `AgentRun`, and dispatches `builtin_agent_execute_task.delay()` with correct args.                         |
| agent-ux.AC3.4 (service messages)       | unit      | `apps/web/hw/services/agent-conversation.service.test.ts`      | 8     | `listMessages` calls correct endpoint and returns messages array.                                                                                                              |
| agent-ux.AC3.4 (store messages)         | unit      | `apps/web/hw/store/agent/agent-conversation.store.test.ts`     | 8     | `fetchMessages` populates `messagesByConversationId` for the given conversation.                                                                                               |
| agent-ux.AC3.4 (panel message list)     | component | `apps/web/hw/components/agent/chat/agent-chat-panel.test.tsx`  | 8     | Message list renders all messages from the active conversation in chronological order.                                                                                         |
| agent-ux.AC3.4 (multi-turn context)     | unit      | `apps/api/plane/tests/unit/hw/test_llm_client.py`              | 4     | `AgentLLMClient._build_history_message()` with `thinking_blocks` preserves them in the output dict. Verify the message format supports multi-turn context for Anthropic's API. |
| agent-ux.AC3.4 (conversation messages)  | contract  | `apps/api/plane/tests/contract/hw/test_agent_conversations.py` | 4     | Sending a second message to an existing conversation passes previous conversation messages as context to the Celery task.                                                      |
| agent-ux.AC3.5 (elicitation)            | component | `apps/web/hw/components/agent/chat/agent-chat-panel.test.tsx`  | 8     | When activities include an elicitation type, `ElicitationCard` renders with a submit handler.                                                                                  |
| agent-ux.AC3.7 (permission error)       | component | `apps/web/hw/components/agent/chat/agent-chat-panel.test.tsx`  | 8     | When activities include a permission error (`activity_type: "error"` with permission-related content), the error renders with user-friendly messaging.                         |
| agent-ux.AC3.7 (permission denied)      | contract  | `apps/api/plane/tests/contract/hw/test_agent_conversations.py` | 4     | Sending a message as a user who is not a workspace member returns 403.                                                                                                         |
| agent-ux.AC3.9 (CE stub)                | component | `apps/web/hw/components/agent/chat/agent-chat-panel.test.tsx`  | 8     | CE stubs for `AgentChatPanel` and `ChatTriggerButton` render null.                                                                                                             |

### agent-ux.AC4: Built-in agent runtime

| Criterion                               | Type | Test File                                                 | Phase | Description                                                                                                                                                                                                                         |
| --------------------------------------- | ---- | --------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| agent-ux.AC4.1 (sandbox IPC)            | unit | `apps/api/plane/tests/unit/hw/test_sandbox.py`            | 3     | Code calling `callTool("issues.list", {...})` produces a `tool_call` IPC message, receives a `tool_result`, and returns output. Tool registry is mocked for unit isolation.                                                         |
| agent-ux.AC4.1 (full loop)              | unit | `apps/api/plane/tests/unit/hw/test_builtin_agent_task.py` | 4     | Integration test: LLM returns code, sandbox executes, tool calls route through registry, response activity is created, and the run completes. LiteLLM is mocked; sandbox may be real or mocked.                                     |
| agent-ux.AC4.1 (IPC protocol)           | unit | `apps/api/plane/tests/unit/hw/test_sandbox.py`            | 3     | `parse_sandbox_message` correctly parses valid JSON lines. Invalid JSON raises `ValueError`. Missing `type` field raises `ValueError`. `serialize_host_message` produces valid JSON with newline.                                   |
| agent-ux.AC4.2 (activity types)         | unit | `apps/api/plane/tests/unit/hw/test_builtin_agent_task.py` | 4     | A successful execution creates activities in order: `thought` (if reasoning present), `action` (code block), `response` (output). Each has the correct `activity_type` and non-empty content.                                       |
| agent-ux.AC4.3 (issue comment)          | unit | `apps/api/plane/tests/unit/hw/test_builtin_agent_task.py` | 4     | When the run has an `issue_id`, a `response` activity auto-creates an `IssueComment` with `external_source="agent"`, `actor` set to the agent's bot user, and `comment_html` containing the response.                               |
| agent-ux.AC4.4 (doc generation)         | unit | `apps/api/plane/tests/unit/hw/test_tool_registry.py`      | 2     | `generate_docs()` produces markdown containing each registered tool's name, description, and parameter table.                                                                                                                       |
| agent-ux.AC4.4 (type generation)        | unit | `apps/api/plane/tests/unit/hw/test_tool_registry.py`      | 2     | `generate_types()` produces TypeScript `declare function` statements with correct parameter types and return types.                                                                                                                 |
| agent-ux.AC4.4 (full tool set)          | unit | `apps/api/plane/tests/unit/hw/test_agent_tools.py`        | 2     | After importing all tool modules, `list_tools()` returns ~18 tools. `generate_docs()` and `generate_types()` include all of them.                                                                                                   |
| agent-ux.AC4.4 (tool registration)      | unit | `apps/api/plane/tests/unit/hw/test_tool_registry.py`      | 2     | Registering a tool via `@tool(...)` makes it accessible via `get_tool()` and `list_tools()`.                                                                                                                                        |
| agent-ux.AC4.4 (tool functionality)     | unit | `apps/api/plane/tests/unit/hw/test_agent_tools.py`        | 2     | Functional tests: `issues.create` creates an Issue, `issues.list` returns filtered issues, `comments.create` creates an IssueComment, `cycles.add_issues` creates CycleIssue records, etc.                                          |
| agent-ux.AC4.5 (code size)              | unit | `apps/api/plane/tests/unit/hw/test_sandbox.py`            | 3     | Code exceeding `max_code_size` is rejected before the subprocess is spawned. `SandboxResult` has error set.                                                                                                                         |
| agent-ux.AC4.5 (tool call count)        | unit | `apps/api/plane/tests/unit/hw/test_sandbox.py`            | 3     | Code making more than `max_tool_calls` tool calls is killed after the limit, with appropriate error in result.                                                                                                                      |
| agent-ux.AC4.5 (output size)            | unit | `apps/api/plane/tests/unit/hw/test_sandbox.py`            | 3     | Code producing more than `max_output_size` output is killed, with appropriate error in result.                                                                                                                                      |
| agent-ux.AC4.5 (timeout)                | unit | `apps/api/plane/tests/unit/hw/test_sandbox.py`            | 3     | Code with an infinite loop is killed after `timeout_seconds`. `SandboxResult.timed_out` is `True`.                                                                                                                                  |
| agent-ux.AC4.5 (memory)                 | unit | `apps/api/plane/tests/unit/hw/test_sandbox.py`            | 3     | The Deno command includes `--v8-flags=--max-old-space-size={max_memory_mb}`. Memory enforcement is delegated to V8; the test verifies the flag is passed correctly.                                                                 |
| agent-ux.AC4.5 (constraints config)     | unit | `apps/api/plane/tests/unit/hw/test_sandbox.py`            | 3     | `SandboxConstraints` validation methods: `validate_code_size` raises for oversized code, `check_tool_call_count` raises at limit, `check_output_size` raises at limit.                                                              |
| agent-ux.AC4.6 (permission error)       | unit | `apps/api/plane/tests/unit/hw/test_tool_registry.py`      | 2     | `execute()` with a handler that raises `PermissionError` returns `{"error": "Permission denied: <message>"}` -- no exception raised.                                                                                                |
| agent-ux.AC4.6 (tool-level perms)       | unit | `apps/api/plane/tests/unit/hw/test_agent_tools.py`        | 2     | Calling `issues.create` with a user who is not a project member (or is a guest) returns a permission error. Calling with a workspace admin (no project membership) succeeds.                                                        |
| agent-ux.AC4.6 (check helpers)          | unit | `apps/api/plane/tests/unit/hw/test_tool_registry.py`      | 2     | `check_workspace_member` raises `PermissionError` for non-members, passes for active members. `check_project_member` raises for non-members/non-admins, passes for project members and workspace admins.                            |
| agent-ux.AC4.7 (sandbox timeout)        | unit | `apps/api/plane/tests/unit/hw/test_sandbox.py`            | 3     | Timeout kills the subprocess. `SandboxResult.timed_out` is `True`. No exception raised.                                                                                                                                             |
| agent-ux.AC4.7 (task timeout)           | unit | `apps/api/plane/tests/unit/hw/test_builtin_agent_task.py` | 4     | When the sandbox times out, the run transitions to `failed` and an `error` activity is created with a timeout message.                                                                                                              |
| agent-ux.AC4.8 (unknown tool, registry) | unit | `apps/api/plane/tests/unit/hw/test_tool_registry.py`      | 2     | `execute()` with an unknown tool name returns `{"error": "Unknown tool: <name>"}` -- no exception raised.                                                                                                                           |
| agent-ux.AC4.8 (unknown tool, sandbox)  | unit | `apps/api/plane/tests/unit/hw/test_sandbox.py`            | 3     | Code calling a non-existent tool name receives a `tool_error` response. The sandbox handles this gracefully (executor does not crash).                                                                                              |
| agent-ux.AC4.9 (stale detection)        | unit | `apps/api/plane/tests/unit/hw/test_builtin_agent_task.py` | 4     | A built-in agent run that has been `in_progress` with `last_activity_at` older than the stale timeout is marked as `stale` by `detect_stale_agent_runs`. Verifies that runs with a `conversation` FK are not accidentally excluded. |

### agent-ux.AC5: SSE streaming

| Criterion                             | Type     | Test File                                            | Phase | Description                                                                                                                                                                                        |
| ------------------------------------- | -------- | ---------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| agent-ux.AC5.1 (event emission)       | unit     | `apps/api/plane/tests/unit/hw/test_agent_events.py`  | 5     | `emit_activity_event()` calls `send_event()` with the correct channel name and serialised activity data. `send_event` is mocked.                                                                   |
| agent-ux.AC5.1 (both agent types)     | unit     | `apps/api/plane/tests/unit/hw/test_agent_events.py`  | 5     | `emit_activity_event()` is called from both the external agent activity creation path (`AgentRunActivityViewSet.create()`) and the built-in agent Celery task. Verified via integration in task 3. |
| agent-ux.AC5.1 (graceful failure)     | unit     | `apps/api/plane/tests/unit/hw/test_agent_events.py`  | 5     | `emit_activity_event()` handles `send_event()` failure gracefully -- no exception raised, failure is logged.                                                                                       |
| agent-ux.AC5.2 (run channel)          | unit     | `apps/api/plane/tests/unit/hw/test_agent_events.py`  | 5     | `emit_activity_event()` for a run without a conversation only emits on the run-level channel `agent-run-{run_id}`.                                                                                 |
| agent-ux.AC5.3 (conversation channel) | unit     | `apps/api/plane/tests/unit/hw/test_agent_events.py`  | 5     | `emit_activity_event()` for a run with a conversation emits on both the run-level channel and `agent-conversation-{conversation_id}`.                                                              |
| agent-ux.AC5.4 (unauthenticated)      | contract | `apps/api/plane/tests/contract/hw/test_agent_sse.py` | 5     | Unauthenticated GET request to `/api/workspaces/{slug}/agent-runs/{id}/events/` returns 401 or 403.                                                                                                |
| agent-ux.AC5.5 (wrong workspace)      | contract | `apps/api/plane/tests/contract/hw/test_agent_sse.py` | 5     | Authenticated GET request to an SSE endpoint for a run in a workspace the user does not belong to returns 403.                                                                                     |
| agent-ux.AC5 (catch-up)               | contract | `apps/api/plane/tests/contract/hw/test_agent_sse.py` | 5     | Activities created while no SSE connection exists are retrievable via the activity list endpoint. Create several activities, then verify they are all returned in `created_at` order.              |
| agent-ux.AC5 (run status event)       | unit     | `apps/api/plane/tests/unit/hw/test_agent_events.py`  | 5     | `emit_run_status_event()` emits on both run-level and conversation-level channels with event type `run_status_changed` and the correct status data.                                                |

### agent-ux.AC6: Cross-cutting behaviours

| Criterion                           | Type      | Test File                                                                                                            | Phase | Description                                                                                                                                                                                                  |
| ----------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| agent-ux.AC6.1 (auth required)      | contract  | `apps/api/plane/tests/contract/hw/test_agent_conversations.py`, `apps/api/plane/tests/contract/hw/test_agent_sse.py` | 4, 5  | All new API endpoints (conversation CRUD, message send, SSE) reject unauthenticated requests.                                                                                                                |
| agent-ux.AC6.2 (external unchanged) | unit      | `apps/api/plane/tests/unit/hw/test_agent_models.py`                                                                  | 1     | Creating an `AgentProfile` without specifying `agent_type` defaults to `"external"`. Existing agent infrastructure (runs, activities) works unchanged with the new field present.                            |
| agent-ux.AC6.2 (mention routing)    | contract  | `apps/api/plane/tests/contract/hw/test_agent_conversations.py`                                                       | 4     | When a comment @mentions an external agent, `agent_webhook_send_task` is dispatched (unchanged from pre-existing behaviour).                                                                                 |
| agent-ux.AC6.3 (store stubs)        | unit      | `apps/web/hw/store/agent/agent-profile.store.test.ts`, `apps/web/hw/store/agent/agent-conversation.store.test.ts`    | 6, 8  | CE root store instantiates `AgentProfileStore` and `AgentConversationStore` identically to the HW root store. Both builds have `rootStore.agentProfileStore` and `rootStore.agentConversationStore`.         |
| agent-ux.AC6.3 (component stubs)    | component | `apps/web/hw/components/agent/chat/agent-chat-panel.test.tsx`                                                        | 8     | CE stubs for `AgentChatPanel` and `ChatTriggerButton` render null. CE stubs for agent settings components (`AgentsList`, `CreateAgentModal`, `AgentDetailRoot`) render null.                                 |
| agent-ux.AC6.3 (mention stub)       | unit      | `apps/web/hw/hooks/use-additional-editor-mention.test.tsx`                                                           | 7     | CE stub returns empty sections regardless of `enableAdvancedMentions` flag. `editorMentionTypes` returns `["user_mention"]` only.                                                                            |
| agent-ux.AC6.4 (dual trigger)       | contract  | `apps/api/plane/tests/contract/hw/test_agent_conversations.py`                                                       | 4     | The built-in agent is triggered via both the conversation message endpoint (chat UI path) and @mention in comments (mention path). Both dispatch `builtin_agent_execute_task` with appropriate trigger type. |

---

## Human verification required

| Criterion                                 | Why Not Automated                                                                                                                                          | Verification Approach                                                                                                                                                                                                                                                                                 |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| agent-ux.AC4.1 (Deno sandbox end-to-end)  | Requires Deno binary installed in the test environment. CI may not have Deno.                                                                              | Sandbox integration tests are marked with `@pytest.mark.skipif(not shutil.which("deno"), reason="Deno not installed")`. Manual verification: run sandbox tests in a Docker container with Deno installed (matching production image). Verify code executes, tool calls route, and output is returned. |
| agent-ux.AC4.5 (memory limit enforcement) | V8 memory limits are enforced by the Deno runtime, not by Python code. The automated test only verifies the flag is passed.                                | Manual verification: run a Deno sandbox script that allocates memory beyond the limit and confirm the process is killed by V8 with an out-of-memory error.                                                                                                                                            |
| agent-ux.AC5.1 (live SSE stream)          | `django-eventstream` SSE connections require a running ASGI server with Redis pub/sub. The Django test client does not support long-lived SSE connections. | Manual verification: start the full stack (`docker compose up`), open an `EventSource` connection to the SSE endpoint in a browser or via `curl`, trigger an agent run, and confirm events stream in real-time. Verify both run-level and conversation-level channels deliver events.                 |
| agent-ux.AC5 (connection drop recovery)   | SSE reconnection is a client-side (frontend) concern that requires a live SSE connection to test.                                                          | Manual verification: open an SSE connection, disconnect it (network interruption or server restart), reconnect, and confirm the catch-up fetch returns all missed activities before the stream resumes.                                                                                               |
| agent-ux.AC3.4 (full multi-turn)          | True multi-turn verification requires an actual LLM call where the second response references the first exchange. Automated tests mock the LLM.            | Manual verification: start a conversation with the built-in agent, send two messages, and verify the second response accounts for the first exchange. Inspect conversation messages in the database to confirm context is passed correctly.                                                           |
| agent-ux.AC4.9 (stale detection timing)   | The Celery beat schedule (`detect_stale_agent_runs` every minute) requires a running Celery beat process to verify timing.                                 | Manual verification: create an in-progress agent run, wait for the stale timeout, and confirm Celery beat marks it as stale. Automated test calls the function directly (verifying logic, not scheduling).                                                                                            |
| agent-ux.AC1.1 (visual layout)            | The agents list page layout, card styling, type badges, and active status indicators require visual inspection.                                            | Manual verification: navigate to `/settings/agents/` as an admin user. Verify the page lists all agents with display name, type badge ("Built-in" / "External"), and active/inactive status indicator. Confirm the built-in agent card is visually distinct.                                          |
| agent-ux.AC1.2 (token modal)              | The two-phase create modal (form then token display) requires visual inspection of the modal flow.                                                         | Manual verification: open the create agent modal, fill in the form, submit, and verify the API token is displayed once with copy-to-clipboard and a warning. Close and confirm the token is no longer accessible.                                                                                     |
| agent-ux.AC1.3 (detail page layout)       | The agent detail page with editable form, configuration fields, and runs table requires visual verification.                                               | Manual verification: navigate to an agent detail page. Verify editable fields render correctly, the runs table shows recent runs with status/trigger/issue/timestamps, and save persists changes.                                                                                                     |
| agent-ux.AC1.4 (built-in card UI)         | The built-in agent card with model override, tool permissions, active toggle, and "cannot delete" constraint is a visual/interaction concern.              | Manual verification: view the built-in agent detail page. Confirm configuration options are present, the delete button is absent, and the active toggle works.                                                                                                                                        |
| agent-ux.AC1.5 (deactivation UI)          | The deactivation toggle visual feedback and downstream effect on mention dropdown require a running app.                                                   | Manual verification: deactivate an agent via the toggle, then open the @mention dropdown and confirm the deactivated agent does not appear.                                                                                                                                                           |
| agent-ux.AC1.6 (not authorized page)      | The "Not authorized" view for non-admin users requires visual verification of the correct error page rendering.                                            | Manual verification: log in as a non-admin user and navigate to `/settings/agents/`. Verify the "Not authorized" view renders instead of the agents list.                                                                                                                                             |
| agent-ux.AC2.1 (dropdown visual)          | The "Agents" section appearing alongside "Users" in the mention dropdown requires a running editor instance.                                               | Manual verification: open a rich text editor, type `@`, and verify agents appear in a separate "Agents" section below the "Users" section with bot icons.                                                                                                                                             |
| agent-ux.AC2.2 (filter visual)            | Live filtering of agent suggestions as the user types requires a running editor with backend search.                                                       | Manual verification: type `@ag` in the editor and verify only matching agent names appear with bot icon and type badge.                                                                                                                                                                               |
| agent-ux.AC2.3 (insertion visual)         | The `@agent-name` insertion into editor content requires a running TipTap editor.                                                                          | Manual verification: select an agent from the dropdown and verify `@agent-name` is inserted as a mention node in the editor content.                                                                                                                                                                  |
| agent-ux.AC3.1 (panel visual)             | The right sidebar panel layout, slide animation, persistence across navigation, and resizability require visual verification.                              | Manual verification: click the trigger button, verify the panel slides in from the right. Navigate to different pages and confirm the panel stays open. Resize the panel if drag handle is implemented.                                                                                               |
| agent-ux.AC3.3 (streaming visual)         | Real-time SSE streaming of thoughts, code, tool calls, and responses into the chat panel requires a running full stack.                                    | Manual verification: send a message in the chat panel that triggers tool use. Verify thoughts appear as collapsible muted blocks, code blocks render with syntax highlighting, tool results appear, and the final response streams in progressively.                                                  |
| agent-ux.AC3.5 (elicitation visual)       | Elicitation card rendering and response submission within the chat panel require a running agent that triggers elicitation.                                | Manual verification: trigger a scenario where the agent requests clarification. Verify the elicitation card renders with input fields and a submit button, and that submitting continues the agent's execution.                                                                                       |
| agent-ux.AC3.6 (cross-workspace ops)      | Workspace-wide operations from the chat panel require a running agent with real tool execution.                                                            | Manual verification: ask the agent to create an issue, query a cycle, or assign a user from the chat panel. Verify the operations succeed and the results are visible in the main UI.                                                                                                                 |
| agent-ux.AC3.8 (SSE reconnection)         | SSE connection drop and recovery in the chat panel requires simulating network interruption with a running full stack.                                     | Manual verification: open the chat panel with active SSE. Disconnect the network (or stop the server briefly). Reconnect and verify the panel recovers -- fetches missed activities and resumes streaming.                                                                                            |

---

## Test file summary

| Test File                                                      | Type        | Phase | Criteria Covered                                              |
| -------------------------------------------------------------- | ----------- | ----- | ------------------------------------------------------------- |
| `apps/api/plane/tests/unit/hw/test_agent_models.py`            | unit        | 1     | AC1.4, AC6.2                                                  |
| `apps/api/plane/tests/unit/hw/test_builtin_agent_seed.py`      | unit        | 1     | AC1.4                                                         |
| `apps/api/plane/tests/unit/hw/test_tool_registry.py`           | unit        | 2     | AC4.4, AC4.6, AC4.8                                           |
| `apps/api/plane/tests/unit/hw/test_agent_tools.py`             | unit        | 2     | AC4.4, AC4.6                                                  |
| `apps/api/plane/tests/unit/hw/test_sandbox.py`                 | unit        | 3     | AC4.1, AC4.5, AC4.7, AC4.8                                    |
| `apps/api/plane/tests/unit/hw/test_builtin_agent_task.py`      | unit        | 4     | AC4.1, AC4.2, AC4.3, AC4.7, AC4.9, AC2.6                      |
| `apps/api/plane/tests/unit/hw/test_llm_client.py`              | unit        | 4     | AC3.4                                                         |
| `apps/api/plane/tests/contract/hw/test_agent_conversations.py` | contract    | 4     | AC3.2, AC3.4, AC3.7, AC2.4, AC2.5, AC2.7, AC6.1, AC6.2, AC6.4 |
| `apps/api/plane/tests/unit/hw/test_agent_events.py`            | unit        | 5     | AC5.1, AC5.2, AC5.3                                           |
| `apps/api/plane/tests/contract/hw/test_agent_sse.py`           | contract    | 5     | AC5.4, AC5.5, AC6.1                                           |
| `apps/web/hw/services/agent.service.test.ts`                   | unit        | 6, 7  | AC1.1, AC1.2, AC1.3, AC1.5, AC2.2                             |
| `apps/web/hw/store/agent/agent-profile.store.test.ts`          | unit/integ  | 6     | AC1.1, AC1.2, AC1.5, AC1.6, AC1.7, AC6.3                      |
| `apps/web/hw/hooks/use-additional-editor-mention.test.tsx`     | unit        | 7     | AC2.1, AC2.2, AC2.3, AC2.7, AC2.8, AC6.3                      |
| `apps/web/hw/hooks/use-editor-mention-integration.test.tsx`    | integration | 7     | AC2.1, AC2.3                                                  |
| `apps/api/plane/tests/unit/test_agent_mentions.py`             | unit        | 7     | AC2.4, AC2.5, AC2.7                                           |
| `apps/web/hw/services/agent-conversation.service.test.ts`      | unit        | 8     | AC3.2, AC3.4                                                  |
| `apps/web/hw/store/agent/agent-conversation.store.test.ts`     | unit        | 8     | AC3.1, AC3.2, AC3.4, AC6.3                                    |
| `apps/web/hw/components/agent/chat/agent-chat-panel.test.tsx`  | component   | 8     | AC3.1, AC3.2, AC3.4, AC3.5, AC3.7, AC3.9, AC6.3               |
